{
  "name": "system-emojis-revenge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node --watch-path=src --watch-path=manifest.json build.mjs"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
